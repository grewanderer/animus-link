# Conformance & Test Vectors

All normative behavior requires test vectors.

Vectors are stored in `conformance/vectors/*.json` and validated by `conformance/schema/vectors.schema.json`.

MVP vectors:
- handshake_noise_xx
- framing_aead
- anti_replay_window
- discovery_records
- relay_ctrl
