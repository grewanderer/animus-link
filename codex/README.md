# Codex Agent Playbooks

Each agent owns a coherent slice of the repo. Agents must:
- Follow `spec/*` and update it when changing protocol behavior.
- Add/extend conformance vectors for any wire-visible change.
- Add tests.

See `codex/agents/*` and `codex/tasks/*`.
