{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "Default desktop capability for the main operator console window.",
  "windows": ["main"],
  "permissions": ["core:default", "http:default"]
}
